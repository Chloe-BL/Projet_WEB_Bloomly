{% extends 'layout.twig' %}

{% block title %}Vos Offres{% endblock %}


{% block body %}

        
    {# SECTION OFFRES #}
    {% if section == 'offres' %}
    <nav class="navbar">
            <a class="active">Vos annonces</a>
        </nav>
         <div class="sous-nav">
         </div>

        <h1 class="police">Consulter vos annonces</h1>
        <p class="police">Retrouvez ici l'ensemble des offres de stage que vous avez publiées.</p>
    <div class="liste">
    {% for item in itemsPage %}
        <div class="bloc-liste">
            <img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce">
            <p>{{item}} </p>
        </div>
    {% endfor %} 
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=offres&connect=oui&p={{ page - 1 }}">Précédent</a>
            {#<a href="index.php?page={{page - 1}}">Précédent</a>#}
        {% else %} 
            <span class="disabled">Précédent</span>
        {% endif %} 


       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %} 
                <a href="index.php?page=choix_section&section=offres&connect=oui&p={{ i }}"> {{ i }} </a>
                {#<a href="index.php?page={{i}}"> {{i}}</a>#}
            {% endif %}
        {% endfor %}

        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=offres&connect=oui&p={{ page + 1 }}">Suivant</a>
            {#<a href="index.php?page={{page + 1}}">Suivant</a>#}
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>  
    </div>
    <a class="acc" href="index.php?page=validationConnexion">Retour à l'accueil</a>

    {# SECTION ENTREPRISES #}
    {% elseif section == 'entreprises' %}
    <nav class="navbar">
            <a class="active">Vos entreprises</a>
        </nav>
        <div class="sous-nav"></div>

        <h1 class="police">Consulter vos entreprises</h1>
        <p class="police">Retrouvez ici l'ensemble des entreprises que vous avez publiées.</p>
    <div class="liste">
    {% for item in itemsPage %}
        <div class="bloc-liste">
            <img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce">
            <p>{{item}} </p>
        </div>
    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=entreprises&connect=oui&p={{ page - 1 }}">Précédent</a>
            {#<a href="index.php?page={{page - 1}}">Précédent</a>#}
        {% else %} 
            <span class="disabled">Précédent</span>
        {% endif %} 


       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %} 
                <a href="index.php?page=choix_section&section=entreprises&connect=oui&p={{ i }}"> {{ i }} </a>
                {#<a href="index.php?page={{i}}"> {{i}}</a>#}
            {% endif %}
        {% endfor %}

        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=entreprises&connect=oui&p={{ page + 1 }}">Suivant</a>
            {#<a href="index.php?page={{page + 1}}">Suivant</a>#}
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion">Retour à l'accueil</a>
    
    {# SECTION ETUDIANTS #}
    {% elseif section == 'etudiants' %}
    <nav class="navbar">
            <a href="index.php?page=mon_espace&connect=oui">Mon espace</a>
            {% if user == 'admin' %}
                <a href="index.php?page=choix_section&section=pilots&user=admin&connect=oui">Comptes Pilotes</a>                
            {% endif %}
            <a class="active">Mes Etudiants</a>
        </nav>
        <div class="sous-nav"></div>

        <br>
        <h1 class="police">Consulter vos étudiants</h1>
        <p class="police">Retrouvez ici l'ensemble des profils étudiants que vous avez créés.</p>
         <a class = "ajout" href= "index.php?page=inscription&section=etudiants&user=admin&connect=oui">Ajouter un compte étudiant</a>
        <div class="recherche-bar-profil">
                    <span class="icon">🔍</span>
                    <input type="text" placeholder="Rechercher des étudiants">
                </div>
    <div class="liste">
    {% for item in itemsPage %}
    <div class="bloc-liste">
        <img src="http://bloomly-static.local/assets/profil.png" alt="Image d'un profil">
        <p>{{ item.prenom }} {{ item.nom }}</p>
    </div>

    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=etudiants&user=admin&user=admin&connect=oui&p={{ page - 1 }}">Précédent</a>
            {#<a href="index.php?page={{page - 1}}">Précédent</a>#}
        {% else %} 
            <span class="disabled">Précédent</span>
        {% endif %} 


       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %} 
                <a href="index.php?page=choix_section&section=etudiants&user=admin&connect=oui&p={{ i }}"> {{ i }} </a>
                {#<a href="index.php?page={{i}}"> {{i}}</a>#}
            {% endif %}
        {% endfor %}

        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=etudiants&user=admin&connect=oui&p={{ page + 1 }}">Suivant</a>
            {#<a href="index.php?page={{page + 1}}">Suivant</a>#}
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui">Retour à l'accueil</a>

    {# SECTION PILOTES #}
    {% elseif section == 'pilots' %}
    <nav class="navbar">
            <a href="index.php?page=mon_espace&connect=oui">Mon espace</a>
            {% if user == 'admin' %}
                <a class="active">Comptes Pilotes</a>              
            {% endif %}
                <a href="index.php?page=choix_section&section=etudiants&user=admin&connect=oui">Mes Etudiants</a>
        </nav>
        <div class="sous-nav"></div>

        <br>
        <h1 class="police">Consulter vos Pilotes</h1>
        <p class="police">Retrouvez ici l'ensemble des profils pilotes que vous avez créés.</p>
        <a class = "ajout" href= "index.php?page=inscription&section=pilots&connect=oui">Ajouter un compte pilote</a>
                <div class="recherche-bar-profil">
                    <span class="icon">🔍</span>
                    <input type="text" placeholder="Rechercher des pilotes">
                </div>
            </div>
    <div class="liste">
    {% for item in itemsPage %}
    <div class="bloc-liste">
        <img src="http://bloomly-static.local/assets/profil.png" alt="Image d'un profil">
        <p>{{ item.prenom }} {{ item.nom }}</p>
    </div>
    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=pilots&user=admin&connect=oui&p={{ page - 1 }}">Précédent</a>
            {#<a href="index.php?page={{page - 1}}">Précédent</a>#}
        {% else %} 
            <span class="disabled">Précédent</span>
        {% endif %} 


       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %} 
                <a href="index.php?page=choix_section&section=pilots&user=admin&connect=oui&p={{ i }}"> {{ i }} </a>
                {#<a href="index.php?page={{i}}"> {{i}}</a>#}
            {% endif %}
        {% endfor %}

        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=pilots&user=admin&connect=oui&p={{ page + 1 }}">Suivant</a>
            {#<a href="index.php?page={{page + 1}}">Suivant</a>#}
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion">Retour à l'accueil</a>

    {% elseif section == 'wishlist' %}
    <nav class="navbar">
            <input type="checkbox" id="interrupteur">
            <label for="interrupteur" class="burger">☰</label>

            <div class="cache">
                <a href="index.php?page=mon_espace&connect=oui">Mon Espace</a>  
                <a class="active">Ma Wishlist</a>  
                {% if user == 'etudiant' %}
                <a href=" ">Mon Agenda</a>
                {% endif %}
            </div>
        </nav>
        <div class="sous-nav"></div>
        <br>
        <h3 class="police">Consulter les offres que vous avez ajoutées à votre wishlist</h3>

        <div class="liste">
    {% for item in itemsPage %}

        <div class="bloc-liste">
            <img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce">
            <p>{{ item}}</p>
        </div>
    {% endfor %}

    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=wishlist&user=etudiant&connect=oui&p={{ page - 1 }}">Précédent</a>
            {#<a href="index.php?page={{page - 1}}">Précédent</a>#}
        {% else %} 
            <span class="disabled">Précédent</span>
        {% endif %} 

       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %} 
                <a href="index.php?page=choix_section&section=wishlist&user=etudiant&connect=oui&p={{ i }}"> {{ i }} </a>
                {#<a href="index.php?page={{i}}"> {{i}}</a>#}
            {% endif %}
        {% endfor %}

        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=wishlist&user=etudiant&connect=oui&p={{ page + 1 }}">Suivant</a>
            {#<a href="index.php?page={{page + 1}}">Suivant</a>#}
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui">Retour à l'accueil</a>


    {% endif %}


{% endblock %}
   