{% extends 'layout.twig' %}
 
{% block title %}Vos Offres{% endblock %}
 
 
{% block body %}
 
       
    {# SECTION OFFRES #}
    {% if section == 'offres' %}
    <nav class="navbar">
            <a class="active">Vos annonces</a>
        </nav>
         <div class="sous-nav">
         </div>
 
        <h1 class="police">Consulter vos annonces</h1>
        {% if user == '1' or user == '2' %}
        <a class = "ajout" href= "index.php?page=ajout_off&section=offres&user={{user}}&connect=oui">Ajouter une offre</a>
        {% endif %}
        <p class="police">Retrouvez ici l'ensemble des offres de stage que vous avez publiées.</p>
    <div class="liste">
    {% for item in itemsPage %}
        <div class="bloc-liste">
            <a href="index.php?page=DescriptionOff&section=offres&connect=oui&user={{user}}&id_offre={{item.id}}&titre={{item.titre}}&p={{ page - 1 }}"><img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce"></a>
            <p>{{item.titre}}</p>

            {% if user == 3 %}
            <form method="post" action="index.php?page=AddFavoris&section=offres&user={{user}}&connect=oui">
                <input type="hidden" name="id_offre" value="{{ item.id }}">
                <input type="hidden" name="titre" value="{{ item.titre}}">
                <button type="submit">Ajouter aux favoris</button>
            </form>
            {% endif %}
        </div>
    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=offres&connect=oui&user={{user}}&p={{ page - 1 }}">Précédent</a>
        {% else %}
            <span class="disabled">Précédent</span>
        {% endif %}
 
 
       {% for i in 1..totalPages|default(1) %}  {# Pour i de 1 à la fin modifier pour bdd!!!#}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=offres&connect=oui&user={{user}}&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=offres&connect=oui&user={{user}}&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>  
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>
 
    {# SECTION ENTREPRISES #}
    {% elseif section == 'entreprises' %}
    <nav class="navbar">
            <a class="active">Vos entreprises</a>
        </nav>
        <div class="sous-nav"></div>
 
        <h1 class="police">Consulter vos entreprises</h1>
        {% if user == '1' or user == '2' %}
            <a class = "ajout2" href= "index.php?page=ajout_ent&section=entreprises&user={{user}}&connect=oui">Ajouter une entreprise</a>
        {% endif %}
        <p class="police">Retrouvez ici l'ensemble des entreprises que vous avez publiées.</p>
    <div class="liste">
    {% for item in itemsPage %}
        <div class="bloc-liste">
            <a href="index.php?page=DescriptionEnt&section=entreprises&connect=oui&user={{user}}&id_entreprise={{item.id_entreprise}}&nom={{item.nom}}&p={{ page - 1 }}"><img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce"></a>
            <p>{{item.nom}}</p>
        </div>
    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=entreprises&connect=oui&user={{user}}&p={{ page - 1 }}">Précédent</a>
        {% else %}
            <span class="disabled">Précédent</span>
        {% endif %}
 
 
       {% for i in 1..totalPages|default(1) %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=entreprises&connect=oui&user={{user}}&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=entreprises&connect=oui&user={{user}}&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>
   
    {# SECTION ETUDIANTS #}
    {% elseif section == 'etudiants' %}
    <nav class="navbar">
                <input type="checkbox" id="interrupteur">
                <label for="interrupteur" class="burger">☰</label>
 
                <div class="cache">
                    <a href="index.php?page=mon_espace&connect=oui&user={{user}}">Mon Espace</a>
                    {% if user == 1 %}
                <a href="index.php?page=choix_section&section=pilots&user={{user}}&connect=oui">Comptes Pilotes</a>                
            {% endif %}
            <a class="active">Mes Etudiants</a>  
                </div>
        </nav>
            <div class="sous-nav"></div>
 
        <br>
        <h1 class="police">Consulter vos étudiants</h1>
        <p class="police">Retrouvez ici l'ensemble des profils étudiants que vous avez créés.</p>
         <a class = "ajout" href= "index.php?page=inscription&section=etudiants&user={{user}}&connect=oui">Ajouter un compte étudiant</a>
            <div class="recherche-bar-profil">
                <form method="get" action="index.php">
                    <input type="hidden" name="page" value="search">
                    <input type="hidden" name="type" value="etudiant">
                    <input type="hidden" name="user" value="{{ user }}">
                    <input type="hidden" name="connect" value="oui">
                    <span class="icon">🔍</span>
                    <input type="text" name="search" placeholder="Rechercher des étudiants">
                    <button type="submit">OK</button>
                </form>
            </div>
    <div class="liste">
    {% for item in itemsPage %}
    <div class="bloc-liste">
        <a href="index.php?page=description_etu&section=etudiants&connect=oui&user={{user}}&id_etud={{item.id_utilisateur}}"><img src="http://bloomly-static.local/assets/profil.png" alt="Image d'un profil"></a>
        <p>{{ item.nom }} {{ item.prenom }}</p>
        <div  class="poubelle" >
                <a href="index.php?page=SupprimerEtudiant&section=etudiants&user={{user}}&connect=oui&id_etudiant={{item.id_utilisateur}}"><img src="http://bloomly-static.local/assets/poubelle.png" alt="Supprimer"></a>
        </div>
    </div>
 
    {% endfor %}
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=etudiants&user={{user}}&connect=oui&p={{ page - 1 }}">Précédent</a>
        {% else %}
            <span class="disabled">Précédent</span>
        {% endif %}
 
 
       {% for i in 1..totalPages|default(1) %}  {# Pour i de 1 à la fin Pour i de 1 à la fin modifier pour bdd!!#}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=etudiants&user={{user}}&connect=oui&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=etudiants&user={{user}}&connect=oui&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>
 
    {# SECTION PILOTES #}
    {% elseif section == 'pilots' %}
        <nav class="navbar">
                <input type="checkbox" id="interrupteur">
                <label for="interrupteur" class="burger">☰</label>
 
                <div class="cache">
                    <a href="index.php?page=mon_espace&connect=oui&user={{user}}">Mon espace</a>
                    {% if user == 1 %}
                <a class="active">Comptes Pilotes</a>                
            {% endif %}
             <a href="index.php?page=choix_section&section=etudiants&user={{user}}&connect=oui">Mes Etudiants</a>  
                </div>
        </nav>
            <div class="sous-nav"></div>
   
 
        <br>
        <h1 class="police">Consulter vos Pilotes</h1>
        <p class="police">Retrouvez ici l'ensemble des profils pilotes que vous avez créés.</p>
        <a class = "ajout" href= "index.php?page=inscription&section=pilots&user={{user}}&connect=oui">Ajouter un compte pilote</a>
            <div class="recherche-bar-profil">
            <form method="get" action="index.php">
            <input type="hidden" name="page" value="search">
            <input type="hidden" name="type" value="pilote">
            <input type="hidden" name="user" value="{{ user }}">
            <input type="hidden" name="connect" value="oui">
            <span class="icon">🔍</span>
            <input type="text" name="search" placeholder="Rechercher des pilotes">
            <button type="submit">OK</button>
        </form>
            </div>
    <div class="liste">
    {% for item in itemsPage %}
    <div class="bloc-liste">
        <a href="index.php?page=description_pil&section=pilots&connect=oui&user={{user}}&id_pilot={{item.id_utilisateur}}"><img src="http://bloomly-static.local/assets/profil.png" alt="Image d'un profil"></a>
        <p>{{ item.nom }} {{ item.prenom }}</p>
        <div  class="poubelle" >
                <a href="index.php?page=SupprimerPilot&section=pilots&user={{user}}&connect=oui&id_pilot={{item.id_utilisateur}}"><img src="http://bloomly-static.local/assets/poubelle.png" alt="Supprimer"></a>
        </div>
    </div>
    {% endfor %}
        <div class="pagination">
            {% if page > 1 %}
                <a href="index.php?page=choix_section&section=pilots&user={{user}}&connect=oui&p={{ page - 1 }}">Précédent</a>
            {% else %}
                <span class="disabled">Précédent</span>
            {% endif %}
       
 
       {% for i in 1..totalPages|default(1) %}  {# Pour i de 1 à la fin Pour i de 1 à la fin modifier pour bdd!!#}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=pilots&user={{user}}&connect=oui&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=pilots&user={{user}}&connect=oui&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
        </div>
        </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>
 
    {# SECTION WISHLIST #}
    {% elseif section == 'wishlist' %}
    <nav class="navbar">
            <input type="checkbox" id="interrupteur">
            <label for="interrupteur" class="burger">☰</label>
 
            <div class="cache">
                <a href="index.php?page=mon_espace&connect=oui&user={{user}}">Mon Espace</a>  
                <a class="active">Ma Wishlist</a>  
                {% if user == 3 %}
                <a href="index.php?page=choix_section&section=agenda&connect=oui&user={{user}}">Mon Agenda</a>
                {% endif %}
            </div>
        </nav>
        <div class="sous-nav"></div>
        <br>
        <h3 class="police">Consulter les offres que vous avez ajoutées à votre wishlist</h3>
 
        <div class="liste">
    {% for item in itemsPage %}
 
        <div class="bloc-liste">
            <img src="http://bloomly-static.local/assets/annonce.png" alt="Image d'une annonce">
            <p>{{ item.titre}}</p>
            <div  class="poubelle" >
                <a href="index.php?page=SupprimerWishlist&section=wishlist&user={{user}}&connect=oui&id_offre={{item.id_offre}}&titre={{item.titre}}"><img src="http://bloomly-static.local/assets/poubelle.png" alt="Supprimer"></a>
            </div>
        </div>
    {% endfor %}
 
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=wishlist&user={{user}}&connect=oui&p={{ page - 1 }}">Précédent</a>
        {% else %}
            <span class="disabled">Précédent</span>
        {% endif %}
 
       {% for i in 1..totalPages|default(1) %}  {# Pour i de 1 à la fin modifier pour bdd!!#}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=wishlist&user={{user}}&connect=oui&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=wishlist&user={{user}}&connect=oui&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>
 
 
    {% elseif section == 'agenda' %}
    <nav class="navbar">
                <input type="checkbox" id="interrupteur">
                <label for="interrupteur" class="burger">☰</label>
 
                <div class="cache">
                    <a href="index.php?page=mon_espace&connect=oui&user={{user}}">Mon Espace</a>          
                    <a href="index.php?page=choix_section&section=wishlist&user={{user}}&connect=oui">Ma Wishlist</a>
                    <a class="active">Mon Agenda</a>    
                </div>
            </nav>
            <div class="sous-nav"></div>
 
            <br>
            <h3 class="police">Retrouvez ici l'ensemble des offres auxquelles vous avez postulé, leur descritpion et leurs dates de postulation.</h3>
 
<div class="liste">
    {% for item in itemsPage %}
 
        <div class="bloc-petite-liste">
            <p>{{ item.titre}}</p>
            <p>Date de candidature : {{item.date_candidature}}</p>
        </div>
    {% endfor %}
 
    <div class="pagination">
        {% if page > 1 %}
            <a href="index.php?page=choix_section&section=agenda&user={{user}}&connect=oui&p={{ page - 1 }}">Précédent</a>
        {% else %}
            <span class="disabled">Précédent</span>
        {% endif %}
 
       {% for i in 1..totalPages %}  {# Pour i de 1 à la fin #}
            {% if i == page %}
                <span class="active-page"> {{i}}</span>
            {% else %}
                <a href="index.php?page=choix_section&section=agenda&user={{user}}&connect=oui&p={{ i }}"> {{ i }} </a>
            {% endif %}
        {% endfor %}
 
        {% if page < totalPages %}
            <a href="index.php?page=choix_section&section=agenda&user={{user}}&connect=oui&p={{ page + 1 }}">Suivant</a>
        {% else %}
            <span class="disabled">Suivant</span>
        {% endif %}
    </div>
    </div>
    <a class="acc" href="index.php?page=validationConnexion&connect=oui&user={{user}}">Retour à l'accueil</a>

    

    {% endif %}
 
 
{% endblock %}
   